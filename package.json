{
  "name": "stocklin-inventory-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend-react start\"",
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend-react start\"",
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend-react install",
    "build": "npm --prefix frontend-react run build"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
